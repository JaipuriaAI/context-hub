{
  "name": "{{PROJECT_NAME}}",
  "version": "0.1.0",
  "description": "My personal Claude Context Hub — shared AI context across Claude interfaces.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute {{PROJECT_NAME}}-db --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute {{PROJECT_NAME}}-db --remote --file=./migrations/0001_init.sql",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.25.2",
    "agents": "^0.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250313.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
